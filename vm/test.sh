#!/usr/bin/env bash
#
# Placeholder. The end-to-end suite belongs to Task 18; until it lands this
# exits nonzero so that a `make vm-test` that "passed" cannot be mistaken for
# coverage the project does not have yet.
set -euo pipefail

echo "e2e suite lands in Task 18" >&2
exit 1
